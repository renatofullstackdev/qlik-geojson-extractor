{
  "name": "qlik-geojson-extractor",
  "version": "1.0.0",
  "description": "Browser-first Qlik Sense/QIX inspector and PointLayer-to-GeoJSON extractor.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.js && node --check browser/qlik-geojson-extractor.js && node --check examples/*.js && node --check chrome-extension/service-worker.js && node --check chrome-extension/lib/qlik-url.js && node --check chrome-extension/sidepanel/app.js && node --check chrome-extension/core/qlik-geojson-extractor.js",
    "build": "node scripts/build-browser.mjs && node scripts/build-extension.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
